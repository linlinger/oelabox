---
kojid_service: "compile/koji.qslinux.com"
koji_web_service: "HTTP/koji.qslinux.com"
kojira_service: "kojira/koji.qslinux.com"
koji_gc_service: "koji-gc/koji.qslinux.com"

ipa_services:
  - "{{ kojid_service }}"
  - "{{ koji_web_service }}"
  - "{{ kojira_service }}"
  - "{{ koji_gc_service }}" 
